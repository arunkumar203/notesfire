specVersion: "v1"
serviceId: "notes-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "notes-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
